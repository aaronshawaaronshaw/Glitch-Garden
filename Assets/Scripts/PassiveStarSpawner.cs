using UnityEngine;
using System.Collections;

public class PassiveStarSpawner : MonoBehaviour {
	
	// Gives the user an additional star every 12 seconds.
	void Update () {
		if (Time.timeSinceLevelLoad % 12 == 0) {
			
		}
	}
}
